#include "cp_renderer.hpp"

bool GMCpRenderer::render() const {
    VideoService* service = GMManager::get_instance()->get_video_service();
    // service->draw_image(&(sprites->tiles->image), GMRect());
}